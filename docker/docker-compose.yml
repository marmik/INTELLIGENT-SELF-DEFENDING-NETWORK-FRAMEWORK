services:
  dashboard:
    image: isdnf:local
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5010:5010"
    volumes:
      - ..:/app
    command: ["python", "-m", "src.dashboard.app", "--host", "0.0.0.0", "--port", "5010"]
